generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URL")
}

model user {
  id       String  @id @map("_id")
  username String
  password String?

  @@unique([username])
  @@map("auth.user")
}
