generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URL")
}

model user {
  username String
  password String?
  id       String  @id @map("_id")

  @@unique([username])
  @@map("auth.user")
}
