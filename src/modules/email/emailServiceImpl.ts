import {IEmailService} from "./emailService";

export class EmailServiceImpl implements IEmailService {
  async sendMail(): Promise<boolean> {
    // TODO
    throw new Error('Please implements EmailService')
  }
}
